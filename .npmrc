# NestJS / ts-node / Prisma historically don't play well with pnpm's default
# isolated node_modules; hoisted linking keeps everything on one flat tree.
node-linker=hoisted

auto-install-peers=true
strict-peer-dependencies=false

# Keep all pnpm state on the F: work drive
store-dir=F:\.pnpm-store
